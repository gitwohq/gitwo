# Use real tokens only if you want to actually publish when running the REAL workflow via act.
# For dry-run workflow these can be left empty.

HOMEBREW_TAP_GITHUB_TOKEN=
GITHUB_TOKEN=

# Optional signing
COSIGN_PRIVATE_KEY=
COSIGN_PASSWORD=
